# 牧养记（Muyangji）项目设计文档

## 一、文档说明

本文档是「牧养记」游戏项目的**概要设计文档**，主要阐述系统的架构思路、模块划分、数据模型和核心流程，为后续编码开发提供指导。文档中**不包含具体代码实现**，只在必要时以文字描述类的职责和接口方法，具体代码将在开发过程中逐模块编写。

---

## 二、整体架构设计

### 2.1 设计目标

- **低耦合**：各功能模块之间依赖清晰，便于独立开发和测试
- **可扩展**：新增牲畜品种、市场或规则调整，只需修改配置文件
- **数据持久化**：所有游戏数据保存在本地SQLite数据库中，支持存档读档
- **界面与逻辑分离**：FXML负责界面布局，Controller负责事件响应，Service负责业务逻辑

### 2.2 分层架构

系统自上而下分为四个层次，每层职责明确，上层依赖下层：

| 层次 | 职责 | 包含内容 |
|------|------|----------|
| **界面层（View）** | 展示数据和接收用户操作 | FXML布局文件、CSS样式、像素图片资源 |
| **控制层（Controller）** | 处理用户交互事件，调度服务 | 各界面控制器类 |
| **业务层（Service）** | 实现核心游戏逻辑 | 游戏循环、市场交易、牲畜管理、评分计算等服务类 |
| **数据层（Data）** | 数据存储与访问 | 实体类、Mapper接口、SQLite数据库 |

**数据流向**：用户操作 → Controller → Service → Mapper → SQLite，反之查询数据时反向流动。

### 2.3 模块划分

系统划分为以下核心模块：

1. **配置模块**：读取和管理 `app-config.json`，为其他模块提供参数
2. **数据库模块**：管理SQLite连接、建表、提供数据访问基础
3. **玩家模块**：管理玩家资金、饲料、棚舍、天数等状态
4. **牲畜模块**：管理牲畜的购买、生长、繁殖、出售、健康等
5. **市场模块**：管理五大市场的价格生成、波动、价格查询
6. **交易模块**：记录所有买卖和花费，生成交易历史
7. **评分模块**：计算经营评分、管理目标进度
8. **工具模块**：提供价格生成、日期计算、JSON处理等通用功能

### 2.4 配置驱动设计

游戏中的数值参数全部放在 `app-config.json` 中，包括：

- 初始资金、饲料、棚舍容量
- 五大市场的名称、距离等级、价格系数、波动幅度
- 每种牲畜的生长天数、日食量、基础价格、繁殖率
- 棚舍扩建费用阶梯
- 评分公式的权重系数
- 目标列表（类型、目标值、奖励）

这样做的好处是：**调整游戏平衡性不需要改代码，只改配置文件即可**，对非技术用户也友好。

---

## 三、数据库设计（概要）

使用SQLite作为嵌入式数据库，文件位于 `data/muyangji.db`。以下是各表的核心字段说明：

### 3.1 player（玩家表）
存储玩家的状态信息，包括资金、饲料库存、棚舍容量、累计利润、当前天数等。**只有一条记录**（单机游戏）。

### 3.2 livestock（牲畜表）
存储农场中每一头牲畜的完整信息：品种、出生天数、当前年龄、体重、健康度、生长阶段、性别、是否怀孕、购买时的价格和购买市场。

### 3.3 market（市场表）
存储五个市场的静态信息：名称、距离等级（1~5）、基础价格系数、波动系数。**数据预置，不随游戏变化**。

### 3.4 market_price（市场价格表）
存储每一天每个市场每种牲畜的买入价和卖出价。这是最核心的动态数据表，每过一天就会新增一批记录（5个市场 × 4种牲畜 = 20条记录/天）。

### 3.5 transaction（交易记录表）
存储所有资金变动记录：购买牲畜、出售牲畜、买饲料、扩建棚舍、目标奖励等。每条记录包含交易类型、关联牲畜、市场、路费、单价、总价、发生天数等。

### 3.6 goal（目标表）
存储游戏中的阶段性目标及其完成进度。每个目标有类型、目标值、当前进度、奖励金额和完成状态。

### 3.7 表间关系（简述）
- player 与 livestock：一对多，一个玩家拥有多牲畜
- livestock 与 transaction：通过 livestock_id 关联，记录每头牲畜的买卖历史
- market 与 market_price：一对多，一个市场有每日价格历史
- transaction 与 market：通过 market_id 关联，记录每笔交易发生在哪个市场

---

## 四、核心模块设计

### 4.1 配置模块

**职责**：加载 `config/app-config.json`，解析为Java对象，以单例方式供全局使用。

**设计要点**：
- 使用Jackson库解析JSON
- 首次启动若配置文件不存在，自动生成默认配置
- 所有游戏参数（初始值、市场参数、牲畜参数、评分权重）均从此读取

### 4.2 数据库模块

**职责**：管理数据库连接，建表，提供SqlSessionFactory。

**设计要点**：
- 首次启动自动建表（若表不存在）
- 自动插入初始数据（市场数据、默认玩家记录）
- 使用MyBatis-Plus的BaseMapper简化CRUD操作
- 提供统一的SqlSessionFactory供各Service使用

### 4.3 玩家服务

**职责**：管理玩家的状态数据，包括资金、饲料、棚舍、天数等。

**核心方法**：
- 查询玩家信息
- 更新资金（加/减）
- 更新饲料库存
- 更新棚舍容量（扩建）
- 推进天数
- 更新累计利润和总资产

**设计要点**：
- 玩家是单例（只有一条记录）
- 每次修改后立即更新数据库
- 总资产 = 资金 + 所有牲畜的估值总和

### 4.4 牲畜服务

**职责**：管理牲畜的完整生命周期。

**核心方法**：
- 购买牲畜（加入农场）
- 获取所有牲畜（按阶段筛选）
- 更新牲畜状态（年龄增长、体重增长、阶段升级）
- 繁殖逻辑（检查是否可繁殖，生成幼崽）
- 出售牲畜（从农场移除）
- 检查健康度（饲料不足时下降）

**设计要点**：
- 不同品种的生长天数和日食量不同，从配置文件读取
- 繁殖需要：成年 + 雌性 + 未怀孕 + 随机概率成功
- 繁殖成功后，母畜进入怀孕状态，经过几天后产仔（可设计为立即产仔，简化逻辑）
- 出售牲畜时记录交易，并更新玩家资金

### 4.5 市场服务

**职责**：管理五大市场的价格生成和查询。

**核心方法**：
- 刷新所有市场价格（每日调用）
- 查询指定市场当日价格
- 查询所有市场所有品种的价格（用于市场总览）
- 计算前往某市场的路费（距离等级 × 5金）
- 执行购买（扣钱、加牲畜、记交易）
- 执行出售（加钱、减牲畜、扣路费、记交易）

**设计要点**：
- 价格生成算法：基础价 × 市场系数 × 随机波动（受波动系数控制）
- 买入价和卖出价独立生成，卖出价通常低于买入价
- 价格有上下限（基础价的50%~200%），防止极端值
- 价格变化用颜色反馈（涨=绿色，跌=红色）
- 每个市场的价格波动幅度不同（清风镇最稳，雪山驿最野）

### 4.6 交易服务

**职责**：记录所有资金变动，提供交易历史查询。

**核心方法**：
- 记录一笔交易
- 按日期查询交易记录
- 按市场统计交易数据
- 计算各市场利润贡献

**设计要点**：
- 每种操作（购买、出售、扩建、买饲料）都记录为一条交易
- 交易记录包含足够信息用于后续统计分析
- 出售交易必须记录市场ID和路费

### 4.7 评分与目标服务

**职责**：计算经营评分，管理目标进度。

**核心方法**：
- 计算综合评分（基于资金、资产、利润、牲畜数、棚舍等级、最远市场、交易次数）
- 获取评分等级（S/A/B/C/D）
- 检查所有目标进度
- 触发目标完成奖励

**设计要点**：
- 评分公式中各项权重可配置
- 目标类型包括：累计利润达标、总资产达标、到达某市场、单笔利润达标等
- 目标完成时弹出庆祝提示并发放奖励

### 4.8 游戏循环服务

**职责**：核心游戏引擎，驱动所有状态更新。

**核心方法**：`advanceDay()` —— 推进一天

**执行流程**：
1. 天数 + 1
2. 刷新所有市场价格
3. 遍历所有牲畜：
   - 年龄 + 1
   - 体重增长
   - 消耗饲料（若饲料不足，健康度下降）
   - 检查是否达到成熟天数 → 升级阶段
   - 检查是否可繁殖（成年+雌性+未怀孕+概率）→ 繁殖
4. 生成“今日事件列表”（繁殖、成熟、死亡等）
5. 返回事件列表供界面展示

**设计要点**：
- 所有操作在同一个事务中完成（保证一致性）
- 事件列表用于弹出通知框
- 如果饲料耗尽，牲畜健康每日下降，健康归零则死亡

---

## 五、界面设计（概要）

### 5.1 设计风格

- **整体风格**：像素风格，暖色系，木纹和草地色调
- **配色**：大地色（棕、绿、米白）为主，点缀亮色按钮
- **字体**：像素风格字体（或等宽字体替代）
- **交互反馈**：按钮悬停变色、价格涨跌颜色提示、弹窗通知

### 5.2 主要界面布局

#### 主界面（农场概览）
- **顶部状态栏**：游戏名称 + 当前天数，右对齐显示
- **信息栏**：资金、饲料库存、棚舍容量（当前/最大）、经营评分（一横排展示）
- **中部的牲畜列表**：用表格展示所有牲畜的ID、品种、年龄、体重、健康度、生长阶段、性别。支持多选。
- **底部操作栏**：一排功能按钮（下一天、市场、买饲料、扩建、统计、设置）

#### 市场界面（独立窗口）
- **顶部市场切换**：五个市场名称作为标签按钮，点击切换
- **当前市场信息**：显示市场名称、距离等级、前往路费
- **价格行情表**：表格展示该市场当前四种牲畜的买入价、卖出价、较昨日变化
- **左侧购买区**：品种下拉选择、数量输入、购买按钮
- **右侧出售区**：我的牲畜列表（仅显示成熟牲畜）、当前市场售价、预估路费、预估净得、出售按钮

#### 出售确认弹窗
- 列出所有可选市场，每个市场显示：名称、当前售价、路费、预估净利润
- 高亮推荐市场（净利润最高）
- 用户选择后确认或取消

#### 统计界面（独立窗口）
- 经营评分与等级（大号显示）
- 利润趋势图（折线图）
- 各市场利润贡献（饼图）
- 目标进度列表（带进度条）

### 5.3 界面交互流程

1. **启动游戏**：加载配置 → 连接数据库 → 加载玩家状态 → 加载牲畜列表 → 显示主界面
2. **下一天**：点击按钮 → 调用GameService.advanceDay() → 刷新所有数据 → 弹出事件通知 → 界面更新
3. **进入市场**：点击市场按钮 → 打开市场窗口 → 默认选中清风镇 → 显示价格和牲畜列表
4. **购买牲畜**：选择品种和数量 → 点击购买 → 扣钱、加牲畜 → 刷新界面
5. **出售牲畜**：选择牲畜 → 点击出售 → 弹出市场选择弹窗 → 选择目标市场 → 确认 → 扣路费、加钱、移除牲畜
6. **查看统计**：点击统计按钮 → 打开统计窗口 → 计算并显示评分、图表、目标

---

## 六、数据流程与状态管理

### 6.1 核心数据流向

**用户操作 → Controller → Service → Mapper → SQLite**

举例（出售牲畜）：
1. 用户在MarketController中选择牲畜，点击“出售”
2. 弹出市场选择弹窗，用户选择目标市场
3. MarketController调用MarketService.sellLivestock(marketId, livestockIds)
4. MarketService计算路费和售价，调用LivestockService移除牲畜，调用PlayerService更新资金，调用TransactionService记录交易
5. 各Service调用对应的Mapper更新数据库
6. 返回结果，Controller刷新界面显示

### 6.2 状态管理原则

- **单一数据源**：所有数据以数据库为准，界面只做展示
- **即时持久化**：每次数据变更立即写入数据库，不依赖“保存”操作
- **界面刷新**：每次操作完成后，重新从数据库加载数据并刷新UI
- **事务一致性**：涉及多表更新的操作（如下一天、出售），需保证全部成功或全部回滚

---

## 七、扩展性与维护性设计

### 7.1 配置化设计

所有数值参数（初始值、价格、生长周期、费用等）均在 `app-config.json` 中配置。新增牲畜品种只需在配置中添加条目，无需修改代码。

### 7.2 接口化设计

各服务之间通过接口交互，具体实现类可替换。例如：
- 价格生成算法可以有不同的实现（简单随机、带趋势、带季节等），通过配置切换
- 评分公式也可以配置化，便于调整

### 7.3 事件驱动设计

“下一天”产生的事件（繁殖、成熟、死亡）以事件列表的形式返回，界面可以灵活展示（弹窗、通知栏、日志等），不耦合具体展示方式。

---

## 八、技术选型说明

| 技术 | 选择理由 |
|------|----------|
| Java 21 | LTS版本，稳定且支持现代语法 |
| JavaFX | 适合桌面应用，自带丰富UI组件，与Java无缝集成 |
| Maven | 标准的Java构建工具，依赖管理方便 |
| SQLite | 轻量级嵌入式数据库，无需安装，单文件存储，适合单机游戏 |
| MyBatis-Plus | 简化JDBC操作，自动映射，减少样板代码 |
| Jackson | 成熟稳定的JSON解析库，用于配置文件读写 |
| Lombok | 减少实体类的getter/setter代码 |

---

## 九、开发阶段划分

| 阶段 | 工作内容 | 产出 |
|------|----------|------|
| 准备 | 搭建项目、配置Maven、安装依赖 | 可运行的空白项目 |
| 第一阶段 | 配置文件 + 数据库 + 实体类 + Mapper | 数据层完整，能读写数据库 |
| 第二阶段 | 五大服务（玩家、牲畜、市场、交易、评分） | 业务逻辑完整，可单元测试 |
| 第三阶段 | 主界面 + 市场界面 + 统计界面 | 所有界面可展示 |
| 第四阶段 | 联调 + 完善交互 | 完整可玩的游戏 |
| 第五阶段 | 测试、平衡性调整、打包 | 可发布的JAR包 |

---

## 十、与其他文档的关系

| 文档 | 关系 |
|------|------|
| 需求文档（requirements.md） | 定义“要做什么”，设计文档定义“怎么做” |
| README.md | 面向用户和开发者，介绍项目和快速上手 |
| 设计文档（本文） | 面向开发者，阐述架构和设计思路 |
| 后续代码实现 | 设计文档的落地，逐模块编码实现 |

---
